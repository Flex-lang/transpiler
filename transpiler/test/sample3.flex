Main()
	arr is an array of integers
	x is integer
	flag is boolean
	flag is false
	for every element in arr
		if element == x
			display "FOUND!"
			flag is true
	if flag == false 
		display "NOT FOUND!"

