Main()
	arr is an array of integers
	arr = 15, 20, 19, 5, 3, 4, 6, 8, 20
	x: integer
	input x
	flag: boolean
	flag is false
	for every element in arr
		if element == x
			flag is true
			display "found!"
	if flag == false
		display "not found!"
